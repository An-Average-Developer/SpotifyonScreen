<Window x:Class="SpotifyOnScreen.Features.Updates.Views.ChangelogDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Available"
        Height="500"
        Width="620"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E2E">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1A1A26" Padding="24,18">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="14" Height="14" Fill="#1DB954" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="VersionText"
                             Text="Update Available"
                             FontSize="20"
                             FontWeight="Bold"
                             Foreground="#E0E0E0"/>
                    <TextBlock Text="A new version of Spotify On Screen is ready to download."
                             FontSize="13"
                             Foreground="#A0A0B0"
                             Margin="0,4,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Changelog -->
        <Border Grid.Row="1"
                Background="#2A2A3A"
                Margin="20,16,20,8"
                CornerRadius="8"
                BorderBrush="#3E3E4E"
                BorderThickness="1"
                Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                         Text="What's New:"
                         FontSize="14"
                         FontWeight="SemiBold"
                         Foreground="#E0E0E0"
                         Margin="0,0,0,12"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ChangelogText"
                             TextWrapping="Wrap"
                             FontFamily="Consolas"
                             FontSize="13"
                             Foreground="#C0C0D0"
                             LineHeight="22"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Padding="20,8,20,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                         x:Name="FileSizeText"
                         Text="Download size: Unknown"
                         FontSize="12"
                         Foreground="#A0A0B0"
                         VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                      Content="Not Now"
                      Click="CancelButton_Click"
                      Background="#2A2A3A"
                      Foreground="#E0E0E0"
                      BorderBrush="#3E3E4E"
                      BorderThickness="1"
                      Padding="20,8"
                      Margin="0,0,10,0"
                      Height="36"
                      Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#35354A"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Grid.Column="2"
                      Content="Download &amp; Update"
                      Click="DownloadButton_Click"
                      Background="#1DB954"
                      Foreground="White"
                      BorderThickness="0"
                      Padding="20,8"
                      Height="36"
                      Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2" Color="#000000"/>
                                </Border.Effect>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1ED760"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
